import { Plus_Jakarta_Sans, Fraunces, Inter, Caveat  } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const jakarata = Plus_Jakarta_Sans({
  subsets: ['latin'],
  display: 'swap',
})

export const fraunces = Fraunces({
    subsets: ['latin'],
    display: 'swap',
  })

  export const caveat = Caveat({
    subsets: ['latin'],
    display: 'swap',
  })